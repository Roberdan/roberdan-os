# /ship

Platform-agnostic ship workflow — local CI gate, commit per phase, push, PR with Summary + Test plan, watch CI, merge-commit only. Honors human gates on main.

Logica canonica: `skills/ship/skill.md` in roberdan-os.
