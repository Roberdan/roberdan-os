# Kanban — To Do (queued)

Queued goals. Loaded at session start. The agent-factory (`factory/`) can also pull autonomous tasks from here.

| # | Goal | Note |
|---|---|---|
| G1 | Realize the self-improving/compounding promise NOW | seed learn-loop with real learnings + demonstrate reuse |
| G2 | Agent factory (Convergio-like, no Convergio) — loops overnight autonomously | build `factory/` on current arch (headless claude + launchd + durable queue) |
| G4 | Cross-session context handoff (fresh agent, no context loss) | build `handoff/` — distill session → resumable brief |
| G5 | Always-on roberdan-os (Mac off) + usable from iPhone Claude app | design honest cloud/hosted path |
