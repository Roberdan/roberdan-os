# Microsoft-confidential dossier — never in git (NDA-split, installed into ~/.roberdan-os/private/)
private/

# Kanban card CONTENT is Roberto's live operational/business state (task detail, client/product
# specifics) — local-only, never committed. The tool itself (kb.sh, README.md) stays versioned;
# only the data directories are ignored. Purged from history 2026-07-04 (see CHANGELOG).
kanban/todo/
kanban/doing/
kanban/done/

# The per-repo authorized-queue snapshot written by `kb queue` — session state, not canon:
# it names card ids from the private board. Wildcard, not the single filename, so a queue
# for a new repo cannot arrive untracked-but-committable the way .coda-roberdan-os.md did.
kanban/.coda-*

# Model-choice bench (eval/bench-*): the DERIVED tasks carry real card content (title + DoD +
# acceptance of closed cards) and the RESULTS carry per-run records — both are Roberto's private
# operational state, same split as kanban/ (the deriver/runner are versioned, their DATA is not).
# bench-derive.sh REFUSES to write anywhere that is not gitignored, so these two entries are load-
# bearing, not decorative. The committed fixtures (eval/bench-fixtures/) are synthetic and stay in.
eval/bench-tasks-local/
eval/bench-results/

# The nested private card repo. `kanban/` holds 69 cards with real names and
# clients; the card DIRECTORIES above are excluded because this remote is
# PUBLIC. The nested repo that versions them lives in loco (kb.sh:43-61
# documents what happened the last time cards were addressed through
# RDA_KANBAN: the writes landed on the wrong board). Its .git and its own
# ignore file are bookkeeping for THAT repo and must never be offered to this
# one - untracked here means a stray `git add` cannot reach them.
kanban/.git/
kanban/.gitignore

# Note generate da gbrain — QUI NON CI DEVE ESSERE NIENTE DA IGNORARE.
#
# Il 2026-08-24 un `git add -A` ha portato dentro un commit di questo repo, che e' PUBBLICO,
# due note scritte da gbrain e marcate `visibility: private`: `people/roberto.md` e
# `claude-code/hooks/bash-guard-sh.md`; una terza, `projects/virtualbpmfy27.md`, aspettava
# non tracciata con numeri di PR, percentuali di margine e il nome di un cliente.
#
# La prima risposta fu elencarle qui. Era sbagliata, e la ragione vale la pena scriverla.
# **Si ignora quello che ha diritto di stare nell'albero** e non va versionato: un artefatto
# di build, una cache. Quelle note nell'albero non ci dovevano stare per NIENTE. Ignorarle
# le rendeva invisibili in `git status` senza renderle piu' sicure: sarebbero rimaste li' ad
# accumularsi, e il giorno in cui il gate fosse stato aggirato con `--no-verify` non ci
# sarebbe stato nessun segnale prima del push.
#
# La causa vera era una sola: la sorgente `default` di gbrain — il cervello personale
# (`people/`, `projects/`, `orgs/`) — non aveva **nessun** `local_path`, quindi scriveva
# relativa alla CWD, che quel giorno era un repo pubblico. Ora ce l'ha:
#   ~/.roberdan-os/private/brain    (fuori da qualsiasi worktree git — verificato)
#
# Quindi qui sotto non c'e' un elenco di cartelle, di proposito. Se `people/` o una cartella
# nuova ricompare in questo repo, e' un BUG da vedere — `git status` la mostra, e
# test/private-marker-check.sh la blocca leggendo il marcatore dentro al file. Visibile e
# bloccata batte nascosta e bloccata.

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Loop cursor state (durable, not versioned)
.agent-state/

# Per-platform wrappers — fully generated by `bin/sync.sh --emit-only` from the canon.
# Run it locally whenever you need them on disk; test/validate.sh checks generation is
# deterministic instead of diffing against a committed copy. See bin/sync.sh, README.md.
platforms/

# claude-ai-skill/ zip is a build artifact of the committed roberto-mode/ source folder —
# regenerate with `cd claude-ai-skill && zip -r roberto-mode.zip roberto-mode`.
claude-ai-skill/*.zip

# eval/ harness output — generated by eval/run-eval.sh, eval/judge.sh, eval/report.sh.
# Regenerate on demand; see eval/README.md. Commit a specific eval/results/report.md by hand
# (force-add) if you want to keep a dated snapshot.
eval/results/
.gstack/

# Build artifact — regenerate with the make-pdf skill against docs/roberdan-os-paper-en.md.
docs/*.pdf
.gbrain-source

# Pause/resume checkpoint — ephemeral local runtime state, per-repo (see kb pause/resume).
handoff/resume.md

# Serena (coding-agent toolkit) per-machine project config. Scaffolded on its own
# the first time a session touches this repo with Serena's MCP enabled - it appeared
# untracked mid-run on 2026-07-28 without anyone asking for it. Machine-local tool
# state, not repo canon: an unreviewed config landing in a commit by accident is
# exactly what `git add -A` (already blocked by the pre-commit hook) would do.
# Force-add it by hand if this repo ever deliberately adopts Serena.
.serena/
