# Dossier Microsoft-confidenziale — mai in git (NDA-split, installato in ~/.roberdan-os/private/)
private/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Loop cursor state (durevole, non versionato)
.agent-state/

# Bundle incollabile generato on-demand da bin/make-bundle.sh (derivato dal canone)
platforms/chatgpt/bundle.md
